/* pybnlearn: forwarding header so that the vendored bnlearn sources can
 * include the R headers by their usual names without R being present.
 * All of the R C API that bnlearn uses lives in compat/rcompat.h. */
#include "../rcompat.h"
